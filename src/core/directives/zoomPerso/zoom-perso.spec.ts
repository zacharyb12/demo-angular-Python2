import { ZoomPerso } from './zoom-perso';

describe('ZoomPerso', () => {
  it('should create an instance', () => {
    const directive = new ZoomPerso();
    expect(directive).toBeTruthy();
  });
});
