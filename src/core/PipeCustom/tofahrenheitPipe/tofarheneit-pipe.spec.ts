import { TofarheneitPipe } from './tofarheneit-pipe';

describe('TofarheneitPipe', () => {
  it('create an instance', () => {
    const pipe = new TofarheneitPipe();
    expect(pipe).toBeTruthy();
  });
});
