import { EmojiPipePipe } from './emoji-pipe-pipe';

describe('EmojiPipePipe', () => {
  it('create an instance', () => {
    const pipe = new EmojiPipePipe();
    expect(pipe).toBeTruthy();
  });
});
